# Radar

Site do painel do Radar (prognósticos esportivos). Este repositório guarda só a página publicada (`index.html`), gerada e enviada pela rotina do projeto `radar-de-apostas` do repositório privado CLAUDE-ADM. Não edite aqui.

Probabilidade não é garantia; o painel não lista odds de casas.
